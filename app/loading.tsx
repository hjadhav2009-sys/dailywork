import { LoadingScreen } from "@/components/ui/LoadingScreen";

export default function RootLoading() {
  return <LoadingScreen title="Loading workspace" description="Preparing the DailyWork pick-and-pack interface." />;
}
