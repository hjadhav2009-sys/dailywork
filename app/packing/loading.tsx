import { LoadingScreen } from "@/components/ui/LoadingScreen";

export default function PackingLoading() {
  return <LoadingScreen title="Loading packing tools" description="Preparing AWB scanning, manual lookup, and packing stats." />;
}
